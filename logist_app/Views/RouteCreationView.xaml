<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="logist_app.Views.RouteCreationView"
             xmlns:viewmodels="clr-namespace:logist_app.ViewModels"
             Title="Create Route"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1a1a1a}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="PrimaryColor">#007ACC</Color>
            <Color x:Key="PrimaryColorDark">#5AAFFF</Color>
            <Color x:Key="SuccessColor">#1E8E3E</Color>
            <Color x:Key="SuccessColorDark">#4ADE80</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto, Auto, Auto, *" 
          ColumnDefinitions="*"
          Padding="15"
          RowSpacing="15">

        <Grid Grid.Row="0" ColumnDefinitions="*, Auto, Auto" ColumnSpacing="10">

            <Label Text="Select Clients" 
                   FontSize="22" 
                   FontAttributes="Bold" 
                   TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}"
                   VerticalOptions="Center"/>

            <Button Grid.Column="1"
                    Text="Refresh"
                    Command="{Binding RefreshCommand}"
                    BackgroundColor="{AppThemeBinding Light=White, Dark=#2d2d2d}"
                    TextColor="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark={StaticResource PrimaryColorDark}}"
                    BorderColor="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark={StaticResource PrimaryColorDark}}"
                    BorderWidth="1"
                    CornerRadius="8"
                    HeightRequest="40"
                    Padding="15,0"
                    FontAttributes="Bold"/>

            <Button Grid.Column="2"
                    Text="Create Route"
                    Command="{Binding CreateRouteCommand}"
                    IsEnabled="{Binding IsCreateButtonEnabled}"
                    TextColor="{AppThemeBinding Light=White, Dark=#1a1a1a}"
                    CornerRadius="8"
                    HeightRequest="40"
                    Padding="20,0"
                    FontAttributes="Bold">
                <Button.Triggers>
                    <DataTrigger TargetType="Button" Binding="{Binding IsCreateButtonEnabled}" Value="True">
                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource SuccessColor}, Dark={StaticResource SuccessColorDark}}" />
                    </DataTrigger>
                    <DataTrigger TargetType="Button" Binding="{Binding IsCreateButtonEnabled}" Value="False">
                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#BDBDBD, Dark=#505050}" />
                    </DataTrigger>
                </Button.Triggers>
            </Button>
        </Grid>

        <Border Grid.Row="1" 
                StrokeShape="RoundRectangle 10" 
                StrokeThickness="0" 
                BackgroundColor="{AppThemeBinding Light=White, Dark=#2d2d2d}" 
                Padding="5">
            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,2" Radius="5" Opacity="{AppThemeBinding Light=0.05, Dark=0.2}"/>
            </Border.Shadow>

            <Grid ColumnDefinitions="*, Auto, Auto">
                <SearchBar Grid.Column="0"
                           Placeholder="Search clients..."
                           Text="{Binding FilterText, Mode=TwoWay}"
                           BackgroundColor="Transparent"
                           TextColor="{AppThemeBinding Light=Black, Dark=#E0E0E0}"/>


                <HorizontalStackLayout Grid.Column="2" Spacing="5" Padding="10,0" VerticalOptions="Center">
                    <Label Text="Sort by:" TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}" FontSize="16" VerticalOptions="Center"/>
                    <Picker Title=""
                            TextColor="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark={StaticResource PrimaryColorDark}}"
                            TitleColor="{AppThemeBinding Light=#757575, Dark=#888888}"
                            ItemsSource="{Binding PickerOptions}"
                            SelectedIndex="{Binding SelectedOptionIndex, Mode=TwoWay}"
                            WidthRequest="200"/>
                </HorizontalStackLayout>
            </Grid>
        </Border>

        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding SelectedClientsCollection}"
                        HeightRequest="40"
                        IsVisible="{Binding SelectedClientsCollection.Count, Converter={toolkit:IntToBoolConverter}}">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Horizontal" ItemSpacing="8"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:SelectableClient">
                    <Border StrokeShape="RoundRectangle 20"
                            BackgroundColor="{AppThemeBinding Light=#E6F4EA, Dark=#1a3d2e}"
                            Stroke="{AppThemeBinding Light={StaticResource SuccessColor}, Dark={StaticResource SuccessColorDark}}"
                            StrokeThickness="1"
                            Padding="12,0"
                            HeightRequest="32">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:RouteCreationViewModel}}, Path=ToggleSelectionCommand}"
                                                  CommandParameter="{Binding}" />
                        </Border.GestureRecognizers>

                        <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                            <Label Text="{Binding Name}" TextColor="{AppThemeBinding Light={StaticResource SuccessColor}, Dark={StaticResource SuccessColorDark}}" FontSize="13" FontAttributes="Bold"/>
                            <Label Text="✕" TextColor="{AppThemeBinding Light={StaticResource SuccessColor}, Dark={StaticResource SuccessColorDark}}" FontSize="12" FontAttributes="Bold"/>
                        </HorizontalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <RefreshView Grid.Row="3" Command="{Binding RefreshCommand}">
            <CollectionView ItemsSource="{Binding Clients}"
                            SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="1" VerticalItemSpacing="10"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:SelectableClient">
                        <Border StrokeShape="RoundRectangle 12"
                                StrokeThickness="1"
                                Padding="15">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:RouteCreationViewModel}}, Path=ToggleSelectionCommand}"
                                                      CommandParameter="{Binding}" />
                            </Border.GestureRecognizers>

                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=White, Dark=#2d2d2d}" />
                                    <Setter Property="Stroke" Value="Transparent" />
                                    <Setter Property="Shadow">
                                        <Setter.Value>
                                            <Shadow Brush="Black" Offset="0,2" Radius="3" Opacity="{AppThemeBinding Light=0.05, Dark=0.2}"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#F0FDF4, Dark=#1a3d2e}" />
                                            <Setter Property="Stroke" Value="{StaticResource SuccessColor}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid RowDefinitions="Auto, Auto" ColumnDefinitions="*, Auto" RowSpacing="5">

                                <Label Grid.Row="0" Grid.Column="0" 
                                       Text="{Binding Name}" 
                                       FontSize="16" 
                                       FontAttributes="Bold" 
                                       TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}"/>

                                <Label Grid.Row="0" Grid.Column="1" 
                                       Text="✓" 
                                       TextColor="{AppThemeBinding Light={StaticResource SuccessColor}, Dark={StaticResource SuccessColorDark}}" 
                                       FontSize="20" 
                                       FontAttributes="Bold"
                                       IsVisible="{Binding IsSelected}"/>

                                <VerticalStackLayout Grid.Row="1" Grid.ColumnSpan="2" Spacing="2">

                                    <Label Text="{Binding Address, StringFormat='Address: {0}'}" FontSize="13" TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#B0B0B0}"/>

                                    <HorizontalStackLayout Spacing="10">
                                        <Label Text="{Binding City}" FontSize="15" TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}"/>
                                        <Label Text="•" TextColor="{AppThemeBinding Light=#DDD, Dark=#555}"/>
                                        <Label Text="{Binding Phone, StringFormat='Phone: {0}'}" FontSize="13" TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#B0B0B0}"/>
                                    </HorizontalStackLayout>

                                    <Label Text="{Binding StartDate, StringFormat='Start date: {0:MMM dd, yyyy}'}" 
                                           FontSize="14"
                                           TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#B0B0B0}"
                                           Margin="0,5,0,0"/>
                                </VerticalStackLayout>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

    </Grid>
</ContentPage>
