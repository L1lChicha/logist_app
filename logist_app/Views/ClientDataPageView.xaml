<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="logist_app.Views.ClientDataPageView"
             xmlns:services="clr-namespace:logist_app.Infrastructure.Service"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Title="Clients"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1a1a1a}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <services:IndexConverterService x:Key="IndexConverter" />

            <Color x:Key="PrimaryColor">#007ACC</Color>
            <Color x:Key="PrimaryColorDark">#5AAFFF</Color>
            <Color x:Key="AccentGreen">#1E8E3E</Color>
            <Color x:Key="AccentGreenDark">#4ADE80</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto, Auto, *" Padding="15" RowSpacing="15">

        <Grid Grid.Row="0" ColumnDefinitions="*, Auto">
            <Label Text="Client Database" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}"
                   VerticalOptions="Center"/>

            <Button Grid.Column="1"
                    Text="Refresh"
                    Command="{Binding RefreshCommand}"
                    BackgroundColor="{AppThemeBinding Light=White, Dark=#2d2d2d}"
                    TextColor="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark={StaticResource PrimaryColorDark}}"
                    BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#404040}"
                    BorderWidth="1"
                    HeightRequest="40"
                    CornerRadius="8"
                    Padding="15,0"
                    ToolTipProperties.Text="Refresh list"/>
        </Grid>

        <Border Grid.Row="1" 
                StrokeShape="RoundRectangle 10" 
                StrokeThickness="0" 
                BackgroundColor="{AppThemeBinding Light=White, Dark=#2d2d2d}" 
                Padding="5">
            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,2" Radius="5" Opacity="{AppThemeBinding Light=0.05, Dark=0.2}"/>
            </Border.Shadow>

            <Grid ColumnDefinitions="*, Auto, Auto">
                <SearchBar Grid.Column="0"
                           Placeholder="Search client..."
                           Text="{Binding SearchText}"
                           FontSize="14"
                           BackgroundColor="Transparent"/>

                <Label Grid.Column="1" 
                       VerticalOptions="Center" 
                       Text="Sort by:" 
                       TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}" />
                
                <Picker Grid.Column="2"
                        TitleColor="{AppThemeBinding Light=#757575, Dark=#888888}"
                        ItemsSource="{Binding SortOptions}"
                        SelectedItem="{Binding SelectedSortOption}"
                        WidthRequest="250"
                        FontSize="14"
                        TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}"
                        HorizontalTextAlignment="Center"/>
            </Grid>
        </Border>

        <RefreshView Grid.Row="2" 
                     Command="{Binding RefreshCommand}"
                     RefreshColor="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark={StaticResource PrimaryColorDark}}">

            <CollectionView x:Name="ClientsCollectionView"
                            ItemsSource="{Binding Clients}"  
                            SelectedItem="{Binding SelectedClient, Mode=TwoWay}"
                            SelectionMode="Single">

                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="12"/>
                </CollectionView.ItemsLayout>

                <CollectionView.EmptyView>
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                        <Label Text="[No Clients]" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" TextColor="#CCC"/>
                        <Label Text="No clients found" TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}" FontSize="16"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeShape="RoundRectangle 12" 
                                StrokeThickness="0"
                                BackgroundColor="{AppThemeBinding Light=White, Dark=#2d2d2d}" 
                                Margin="2">
                            <Border.Shadow>
                                <Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="{AppThemeBinding Light=0.05, Dark=0.2}"/>
                            </Border.Shadow>

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup Name="CommonStates">
                                    <VisualState Name="Normal" />
                                    <VisualState Name="Selected">
                                        <VisualState.Setters>
                                            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#F0F7FF, Dark=#1a3a5c}" />
                                            <Setter Property="Stroke" Value="{StaticResource PrimaryColor}" />
                                            <Setter Property="StrokeThickness" Value="1" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <Grid RowDefinitions="Auto, Auto, Auto" RowSpacing="8" Padding="15">

                                <Grid Grid.Row="0" ColumnDefinitions="Auto, *, Auto" ColumnSpacing="10">

                                    <Border Grid.Column="0" 
                                            BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#404040}" 
                                            StrokeShape="RoundRectangle 6" 
                                            Padding="6,2"
                                            VerticalOptions="Start">
                                        <Label Text="{Binding ., 
                                               Converter={StaticResource IndexConverter}, 
                                               ConverterParameter={x:Reference ClientsCollectionView}, 
                                               StringFormat='#{0}'}" 
                                               TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}" 
                                               FontSize="12" 
                                               FontAttributes="Bold"/>
                                    </Border>

                                    <Label Grid.Column="1" 
                                           Text="{Binding Name}" 
                                           FontSize="17" 
                                           FontAttributes="Bold" 
                                           TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}"
                                           LineBreakMode="TailTruncation"
                                           VerticalOptions="Center"/>

                                    <Label Grid.Column="2" 
                                           Text="{Binding Id, StringFormat='ID: {0}'}" 
                                           FontSize="11" 
                                           TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#B0B0B0}"
                                           VerticalOptions="Start"/>
                                </Grid>

                                <HorizontalStackLayout Grid.Row="1" Spacing="6">
                                    <Label Text="Address:" 
                                           FontSize="13" 
                                           FontAttributes="Bold" 
                                           TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}"/>

                                    <Label Text="{Binding Address}" 
                                           FontSize="13" 
                                           TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#B0B0B0}" 
                                           LineBreakMode="TailTruncation"/>

                                    <Label Text="•" TextColor="{AppThemeBinding Light=#CCC, Dark=#666}"/>

                                    <Label Text="{Binding City}" 
                                           FontSize="13" 
                                           TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#B0B0B0}" 
                                           FontAttributes="Italic"/>
                                </HorizontalStackLayout>

                                <BoxView Grid.Row="2" HeightRequest="1" Color="{AppThemeBinding Light=#F5F5F5, Dark=#404040}" VerticalOptions="Start"/>

                                <Grid Grid.Row="2" ColumnDefinitions="*, Auto" Margin="0,5,0,0">

                                    <VerticalStackLayout Grid.Column="0" Spacing="2">
                                        <HorizontalStackLayout Spacing="5">
                                            <Label Text="Phone:" FontSize="12" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}"/>
                                            <Label Text="{Binding Phone}" FontSize="12" TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#B0B0B0}"/>
                                        </HorizontalStackLayout>

                                        <HorizontalStackLayout Spacing="5" IsVisible="{Binding Email, Converter={toolkit:IsStringNotNullOrEmptyConverter}}">
                                            <Label Text="Email:" FontSize="12" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#E0E0E0}"/>
                                            <Label Text="{Binding Email}" FontSize="12" TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#B0B0B0}"/>
                                        </HorizontalStackLayout>
                                    </VerticalStackLayout>

                                    <Border Grid.Column="1" 
                                            Stroke="{AppThemeBinding Light={StaticResource AccentGreen}, Dark={StaticResource AccentGreenDark}}" 
                                            StrokeThickness="1"
                                            StrokeShape="RoundRectangle 8"
                                            BackgroundColor="{AppThemeBinding Light=#F0FDF4, Dark=#1a3d2e}"
                                            Padding="8,4"
                                            VerticalOptions="Center">
                                        <HorizontalStackLayout Spacing="4">
                                            <Label Text="Containers:" 
                                                   FontSize="12" 
                                                   TextColor="{AppThemeBinding Light={StaticResource AccentGreen}, Dark={StaticResource AccentGreenDark}}"/>
                                            <Label Text="{Binding ContainerCount}" 
                                                   FontSize="14" 
                                                   FontAttributes="Bold" 
                                                   TextColor="{AppThemeBinding Light={StaticResource AccentGreen}, Dark={StaticResource AccentGreenDark}}"/>
                                        </HorizontalStackLayout>
                                    </Border>
                                </Grid>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

    </Grid>
</ContentPage>
