<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="logist_app.Views.LoginPageView"
             Title="Вход"
             Shell.NavBarIsVisible="False"
             Shell.FlyoutBehavior="Disabled">
    <ContentPage.Resources>
        <Color x:Key="PrimaryColor">#512BD4</Color>
        <Color x:Key="PrimaryColorDark">#7C5CE7</Color>
        <Color x:Key="SecondaryColor">#2B0B98</Color>
        <Color x:Key="SecondaryColorDark">#1a1a2e</Color>

        <Style TargetType="Entry">
            <Setter Property="TextColor" Value="{AppThemeBinding Light=#333333, Dark=#E0E0E0}" />
            <Setter Property="PlaceholderColor" Value="{AppThemeBinding Light=#999999, Dark=#666666}" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="HeightRequest" Value="50" />
            <Setter Property="BackgroundColor" Value="Transparent" />
        </Style>
    </ContentPage.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark=#1a1a2e}" Offset="0.0" />
                <GradientStop Color="{AppThemeBinding Light={StaticResource SecondaryColor}, Dark=#0d0d1a}" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <Border Stroke="Transparent"
                StrokeThickness="0"
                BackgroundColor="{AppThemeBinding Light=White, Dark=#2d2d2d}"
                VerticalOptions="Center"
                HorizontalOptions="Center"
                WidthRequest="320"
                Padding="30,40"
                Margin="20">

            <Border.StrokeShape>
                <RoundRectangle CornerRadius="25" />
            </Border.StrokeShape>

            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,10" Radius="20" Opacity="{AppThemeBinding Light=0.2, Dark=0.5}" />
            </Border.Shadow>

            <VerticalStackLayout Spacing="20">

                <Label Text="EcoLogistics" 
                       FontSize="28" 
                       FontAttributes="Bold" 
                       TextColor="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark={StaticResource PrimaryColorDark}}" 
                       HorizontalOptions="Center" 
                       Margin="0,0,0,10"/>

                <Label Text="Welcome!" 
                       FontSize="16" 
                       TextColor="{AppThemeBinding Light=Gray, Dark=#888888}" 
                       HorizontalOptions="Center" 
                       Margin="0,-15,0,20"/>

                <Border Stroke="{AppThemeBinding Light=#E0E0E0, Dark=#404040}" 
                        StrokeThickness="1" 
                        StrokeShape="RoundRectangle 10" 
                        BackgroundColor="{AppThemeBinding Light=#F3F3F3, Dark=#1a1a1a}" 
                        Padding="15,0">
                    <Grid ColumnDefinitions="30,*">
                        <Label Text="👤" VerticalOptions="Center" FontSize="18"/>
                        <Entry Grid.Column="1" 
                               x:Name="usernameEntry"
                               Placeholder="Login"
                               Text="{Binding Username}" 
                               Margin="5,0,0,0"/>
                    </Grid>
                </Border>

                <Border Stroke="{AppThemeBinding Light=#E0E0E0, Dark=#404040}" 
                        StrokeThickness="1" 
                        StrokeShape="RoundRectangle 10" 
                        BackgroundColor="{AppThemeBinding Light=#F3F3F3, Dark=#1a1a1a}" 
                        Padding="15,0">
                    <Grid ColumnDefinitions="30,*">
                        <Label Text="🔒" VerticalOptions="Center" FontSize="18"/>
                        <Entry Grid.Column="1" 
                               x:Name="passwordEntry"
                               Placeholder="Password"
                               ReturnCommand="{Binding LoginCommand}"
                               IsPassword="True"
                               Text="{Binding Password}"
                               Margin="5,0,0,0"/>
                    </Grid>
                </Border>

                <Button x:Name="loginButton"
                        Text="Sing in"
                        Command="{Binding LoginCommand}"
                        BackgroundColor="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark={StaticResource PrimaryColorDark}}"
                        TextColor="{AppThemeBinding Light=White, Dark=#1a1a1a}"
                        FontAttributes="Bold"
                        CornerRadius="10"
                        HeightRequest="50"
                        Margin="0,10,0,0">
                    <Button.Shadow>
                        <Shadow Brush="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark={StaticResource PrimaryColorDark}}" 
                                Offset="0,5" 
                                Radius="10" 
                                Opacity="0.4" />
                    </Button.Shadow>
                </Button>

            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>
