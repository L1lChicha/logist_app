<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:logist_app.ViewModels"
             x:Class="logist_app.Views.AppSettingsView"
             x:DataType="vm:AppSettingsViewModel"
             Title="Settings"
             BackgroundColor="{AppThemeBinding Light=#F2F5F9, Dark=#121212}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#2B0B98</Color>

            <Color x:Key="TextPrimary">#1F1F1F</Color>
            <Color x:Key="TextSecondary">#757575</Color>

            <Style x:Key="SettingsCard" TargetType="Border">
                <Setter Property="Padding" Value="20"/>
                <Setter Property="StrokeShape" Value="RoundRectangle 16"/>
                <Setter Property="StrokeThickness" Value="0"/>
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=White, Dark=#1E1E1E}"/>
                <Setter Property="Margin" Value="0,0,0,20"/>
                <Setter Property="Shadow">
                    <Setter.Value>
                        <Shadow Brush="Black" Offset="0,4" Radius="10" Opacity="{AppThemeBinding Light=0.05, Dark=0.3}"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="SectionHeader" TargetType="Label">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource TextPrimary}, Dark=White}"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
            </Style>

            <Style x:Key="ThemeTile" TargetType="Border">
                <Setter Property="HeightRequest" Value="80"/>
                <Setter Property="StrokeShape" Value="RoundRectangle 12"/>
                <Setter Property="StrokeThickness" Value="2"/>
                <Setter Property="Stroke" Value="Transparent"/>
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#F5F7FA, Dark=#2A2A2A}"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="0">

            <Label Text="Settings"
                   FontSize="32"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark=White}"
                   Margin="5,0,0,25"/>

            <Border Style="{StaticResource SettingsCard}">
                <VerticalStackLayout>
                    <Label Text="🎨 Appearance" Style="{StaticResource SectionHeader}"/>
                    <Label Text="Choose your preferred theme" FontSize="13" TextColor="{StaticResource TextSecondary}" Margin="0,-8,0,15"/>

                    <Grid ColumnDefinitions="*,*,*" ColumnSpacing="10">

                        <Border Grid.Column="0" Style="{StaticResource ThemeTile}">
                            <Border.Triggers>
                                <DataTrigger TargetType="Border" Binding="{Binding SelectedThemeIndex}" Value="0">
                                    <Setter Property="Stroke" Value="{StaticResource Primary}"/>
                                    <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#EEF2FF, Dark=#2B2B40}"/>
                                </DataTrigger>
                            </Border.Triggers>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetThemeCommand}" CommandParameter="System"/>
                            </Border.GestureRecognizers>

                            <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="5">
                                <Label Text="⚙️" FontSize="22"/>
                                <Label Text="Auto" FontSize="12" FontAttributes="Bold" TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark=White}"/>
                            </VerticalStackLayout>
                        </Border>

                        <Border Grid.Column="1" Style="{StaticResource ThemeTile}">
                            <Border.Triggers>
                                <DataTrigger TargetType="Border" Binding="{Binding SelectedThemeIndex}" Value="1">
                                    <Setter Property="Stroke" Value="{StaticResource Primary}"/>
                                    <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#EEF2FF, Dark=#2B2B40}"/>
                                </DataTrigger>
                            </Border.Triggers>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetThemeCommand}" CommandParameter="Light"/>
                            </Border.GestureRecognizers>

                            <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="5">
                                <Label Text="☀️" FontSize="22"/>
                                <Label Text="Light" FontSize="12" FontAttributes="Bold" TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark=White}"/>
                            </VerticalStackLayout>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource ThemeTile}">
                            <Border.Triggers>
                                <DataTrigger TargetType="Border" Binding="{Binding SelectedThemeIndex}" Value="2">
                                    <Setter Property="Stroke" Value="{StaticResource Primary}"/>
                                    <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#EEF2FF, Dark=#2B2B40}"/>
                                </DataTrigger>
                            </Border.Triggers>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SetThemeCommand}" CommandParameter="Dark"/>
                            </Border.GestureRecognizers>

                            <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="5">
                                <Label Text="🌙" FontSize="22"/>
                                <Label Text="Dark" FontSize="12" FontAttributes="Bold" TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark=White}"/>
                            </VerticalStackLayout>
                        </Border>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource SettingsCard}">
                <Grid ColumnDefinitions="*, Auto" RowDefinitions="Auto, Auto">
                    <Label Grid.Row="0" Grid.Column="0" Text="🌐 Language" Style="{StaticResource SectionHeader}" Margin="0"/>
                    <Label Grid.Row="1" Grid.Column="0" Text="App interface language" FontSize="13" TextColor="{StaticResource TextSecondary}"/>

                    <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" 
                            StrokeShape="RoundRectangle 8" 
                            BackgroundColor="{AppThemeBinding Light=#F5F7FA, Dark=#2A2A2A}"
                            StrokeThickness="0"
                            Padding="10,0"
                            WidthRequest="130"
                            HeightRequest="40">
                        <Picker Title="Language"
                                ItemsSource="{Binding SupportedLanguages}"
                                ItemDisplayBinding="{Binding Name}"
                                SelectedItem="{Binding SelectedLanguage}"
                                TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark=White}"
                                HorizontalTextAlignment="Center"
                                FontSize="14"/>
                    </Border>
                </Grid>
            </Border>

            <Border Style="{StaticResource SettingsCard}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="About" Style="{StaticResource SectionHeader}"/>

                    <Grid ColumnDefinitions="Auto, *" ColumnSpacing="15">
                        
                            <Label Text="" FontSize="24" HorizontalOptions="Center" VerticalOptions="Center"/>
                        

                        <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                            <Label Text="EcoLogistics App" FontSize="16" FontAttributes="Bold" TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark=White}"/>
                            <Label Text="Version 1.0.0" FontSize="12" TextColor="{StaticResource TextSecondary}"/>
                        </VerticalStackLayout>
                    </Grid>

                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light=#EEE, Dark=#333}" Margin="0,5"/>

                    <Label Text="© 2026 All Rights Reserved" FontSize="12" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Border>

            <Button Text="Log Out"
                    Command="{Binding LogoutCommand}"
                    BackgroundColor="Transparent"
                    TextColor="#FF3B30"
                    BorderColor="#FF3B30"
                    BorderWidth="1"
                    FontSize="16"
                    FontAttributes="Bold"
                    CornerRadius="25"
                    HeightRequest="50"
                    Margin="10,10,10,40">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="PointerOver">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="#15FF3B30" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
            </Button>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>